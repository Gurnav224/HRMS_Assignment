import React from "react";

const Header = () => {
  return (
    <header class="logo-container">
      <div class="logo">
        <div class="logo-square"></div>
        LOGO
      </div>
    </header>
  );
};

export default Header;
